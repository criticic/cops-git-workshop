---
const { post } = Astro.props;
const [username, ...slugParts] = post.slug.split('/');
const slug = slugParts.join('/');
---

<a href={`/${post.slug}`} class="block border-2 border-black p-4 hover:bg-black hover:text-white transition-colors no-underline">
  <div>
    <h2 class="text-2xl font-bold uppercase mb-2">{post.data.title}</h2>
    <p class="text-blue-600 font-bold mb-2">By {username}</p>
    <p class="text-gray-500 mb-4 text-sm">Published on: {new Date(post.data.publishDate).toLocaleDateString('en-IN', { year: 'numeric', month: 'long', day: 'numeric' })}</p>
    <span class="inline-block border-b-2 border-red-500 hover:bg-red-500 hover:text-white">Read More</span>
  </div>
</a>